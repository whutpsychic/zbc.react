export const addnum = v => ({
	type: "ADD_NUMBER",
	value: v
});

export const reducenum = v => ({
	type: "REDUCER_NUMBER",
	value: v
});

export const rendernum = v => ({
	type: "RENDER_NUMBER",
	value: v
});
